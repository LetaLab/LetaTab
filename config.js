// Fallback New Tab URL, used only until you set your own from the
// popup (click the LetaTab icon in the toolbar). Once a URL is saved
// there, it takes over automatically and this value is no longer used.
const NEW_TAB_URL = "https://LetaDial.LetaLab.eu/";
